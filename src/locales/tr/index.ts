import { productsType } from './products';
import { validation } from './validation';

import { navigation } from './navigation';

export const tr = {
  ...productsType,
  ...validation,
  ...navigation,
};
